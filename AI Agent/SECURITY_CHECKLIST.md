# 提交到 GitHub 前的安全检查清单

## ✅ 已完成的安全措施

1. **环境变量隔离**
   - ✅ 所有敏感信息都存储在 `.env` 文件中
   - ✅ `.env` 已添加到 `.gitignore`，不会被提交
   - ✅ `.env.example` 已脱敏，可以安全提交

2. **代码检查**
   - ✅ 代码中没有硬编码的 API Key
   - ✅ 代码中没有硬编码的邮箱地址
   - ✅ 所有配置都通过 `config.py` 从环境变量读取

## 📋 提交前必做步骤

### 1. 初始化 Git 仓库

```bash
git init
git add .
git status  # 确认 .env 文件不在列表中
```

### 2. 检查是否有敏感信息泄露

```bash
# 确保 .env 不会被提交
git status | grep -q ".env$" && echo "⚠️  警告: .env 文件将被提交!" || echo "✅ .env 已被忽略"

# 搜索可能的敏感信息
grep -r "wodemazhenfeijin" --exclude-dir=.venv --exclude=.env . && echo "⚠️  发现邮箱地址!" || echo "✅ 无邮箱泄露"
grep -r "sk-" --exclude-dir=.venv --exclude=.env . && echo "⚠️  发现 API Key!" || echo "✅ 无 API Key 泄露"
grep -r "mpjq" --exclude-dir=.venv --exclude=.env . && echo "⚠️  发现密码!" || echo "✅ 无密码泄露"
```

### 3. 首次提交

```bash
git add .
git commit -m "Initial commit: Daily outfit email agent"
```

### 4. 推送到 GitHub

```bash
# 在 GitHub 上创建新仓库后
git remote add origin https://github.com/你的用户名/你的仓库名.git
git branch -M main
git push -u origin main
```

## ⚠️ 重要提醒

1. **永远不要提交 `.env` 文件**
2. **如果不小心提交了敏感信息**：
   - 立即更换所有泄露的密钥和密码
   - 使用 `git filter-branch` 或 `BFG Repo-Cleaner` 清理历史记录
   - 强制推送覆盖远程仓库

3. **定期检查**：
   ```bash
   git log --all --full-history --source -- .env
   ```
   如果有输出，说明 `.env` 曾被提交过

## 🔒 额外安全建议

1. 使用 GitHub Secrets 存储 CI/CD 所需的敏感信息
2. 考虑使用 `git-secrets` 工具防止意外提交敏感信息
3. 为 Gmail 使用应用专用密码，而不是账户密码
4. 定期轮换 API Keys 和密码
